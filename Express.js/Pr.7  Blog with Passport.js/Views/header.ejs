<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Modern Blog UI</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
      body {
        background: #f5f7fa;
        font-family: "Poppins", sans-serif;
        margin: 0;
        padding: 0;
      }

      /* Top Navigation Bar */
      .topbar {
        background: linear-gradient(90deg, #4e9fd1, #1d3557);
        height: 60px;
        width: 100%;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0 30px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }

      .topbar .logo {
        font-size: 1.6rem;
        font-weight: 700;
        color: #fff;
        text-transform: uppercase;
      }

      .topbar-nav ul {
        list-style: none;
        display: flex;
        margin: 0;
        padding: 0;
      }

      .topbar-item {
        margin-left: 30px;
      }

      .topbar-link {
        color: white;
        text-decoration: none;
        padding: 12px;
        font-size: 1rem;
        font-weight: 500;
        border-radius: 8px;
        display: flex;
        align-items: center;
        transition: all 0.3s ease;
      }

      .topbar-link i {
        margin-right: 10px;
        font-size: 1.3rem;
      }

      .topbar-link:hover {
        background-color: rgba(255, 255, 255, 0.1);
        transform: translateY(-3px);
      }

      .content {
        padding: 50px 30px;
        flex-grow: 1;
        background-color: #f5f7fa;
        text-align: center;
      }

      .content h1 {
        font-size: 2.5rem;
        color: #1d3557;
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        .topbar {
          flex-direction: column;
          align-items: flex-start;
          padding: 20px 30px;
        }

        .topbar-nav ul {
          flex-direction: column;
          align-items: flex-start;
        }

        .topbar-item {
          margin-left: 0;
          margin-bottom: 10px;
        }
      }
    </style>
  </head>
  <body>
    <!-- Topbar -->
    <div class="topbar">
      <div class="logo">My Blog</div>
      <nav class="topbar-nav">
        <ul>
          <li class="topbar-item">
            <a class="topbar-link" href="/dashboard">
              <i class="fas fa-home"></i> Home
            </a>
          </li>
          <li class="topbar-item">
            <a class="topbar-link" href="/addblog">
              <i class="fas fa-plus-circle"></i> Create
            </a>
          </li>
          <li class="topbar-item">
            <a class="topbar-link" href="/logout">
              <i class="fas fa-sign-out-alt"></i> Logout
            </a>
          </li>
        </ul>
      </nav>
    </div>

    <!-- Main Content Section -->
    <div class="content">
      <h1>Welcome to Your Blog Dashboard!</h1>
      <p>Start creating amazing content and share your thoughts with the world.</p>
    </div>

    <!-- Include Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
