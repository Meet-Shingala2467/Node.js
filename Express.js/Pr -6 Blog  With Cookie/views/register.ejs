<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Account</title>
    <style>
        @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@200;300;400;500;600;700;800&display=swap");

        *,
        *::before,
        *::after {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Poppins", sans-serif;
            background-color: #f9fafb; /* Changed background color for a new combination */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .card {
            width: 400px;
            padding: 30px;
            background-color: #fff; /* Kept the card background white for contrast */
            border-radius: 16px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
        }

        .card h2 {
            font-size: 2rem;
            color: #333; /* Changed to a darker color for better contrast */
            margin-bottom: 20px;
        }

        .input-wrap {
            position: relative;
            width: 100%;
            margin-bottom: 1.5rem;
        }

        .input-field {
            width: 100%;
            height: 40px;
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ddd; /* Changed border color for a new combination */
            border-radius: 8px;
            outline: none;
            transition: border-color 0.3s;
        }

        .input-field:focus {
            border-color: #21ba45; /* Changed focus border color for a new combination */
        }

        .input-wrap label {
            position: absolute;
            top: -10px;
            left: 10px;
            font-size: 0.9rem;
            color: #777; /* Changed label color for a new combination */
            background-color: #fff;
            padding: 0 5px;
        }

        .sign-btn {
            width: 100%;
            height: 40px;
            background-color: #21ba45; /* Changed button background color for a new combination */
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s;
            margin-bottom: 20px;
        }

        .sign-btn:hover {
            background-color: #1e8e3e; /* Changed hover background color for a new combination */
        }

        .toggle {
            color: #21ba45; /* Changed toggle color for a new combination */
            font-size: 0.9rem;
            cursor: pointer;
        }

        .text {
            color: #777; /* Changed text color for a new combination */
            font-size: 0.9rem;
        }

        .text a {
            color: #21ba45; /* Changed link color for a new combination */
            text-decoration: none;
        }

        .text a:hover {
            text-decoration: underline;
        }

        .heading {
            margin-bottom: 20px;
        }

        /* For smaller screens */
        @media (max-width: 600px) {
            .card {
                width: 90%;
                padding: 20px;
            }
        }
    </style>
</head>

<body>
    <div class="card">
        <div class="heading">
            <h2>Create Account</h2>
            <p>Already have an account? <a href="/" class="toggle">Sign In</a></p>
        </div>

        <form action="/registerUser" method="post" autocomplete="off">
            <div class="input-wrap">
                <input type="text" name="name" class="input-field" required />
                <label>Name</label>
            </div>
            <div class="input-wrap">
                <input type="email" name="email" class="input-field" required />
                <label>Email</label>
            </div>
            <div class="input-wrap">
                <input type="password" name="password" class="input-field" required />
                <label>Password</label>
            </div>
            <input type="submit" value="Sign Up" class="sign-btn" />
        </form>

        <p class="text">
            By signing up, I agree to the
            <a href="#">Terms of Service</a> and
            <a href="#">Privacy Policy</a>
        </p>
    </div>

</body>

</html>
