<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Showcase</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: #e0f7fa; /* Light cyan background */
            margin: 0;
            background: url('https://img.lovepik.com/bg/20231221/Vintage-Books-Arranged-on-a-Table-Captivating-Background_2662639_wh860.jpg!/fw/860');
            background-size: cover;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            color: #333;
        }

        .container {
            width: 100%;
            max-width: 1300px;
            background: rgba(157, 162, 162, 0.7); /* Set background to transparent cyan */
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            overflow: hidden;
        }

        h1 {
            text-align: center;
            margin-bottom: 40px;
            font-size: 2.5rem;
            color: #bc2c13;; /* Dark teal color */
            font-weight: 600;
        }

        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            gap: 20px;
        }

        .search-bar {
            display: flex;
            flex: 1;
            gap: 12px;
        }

        .search-bar input {
            flex: 1;
            padding: 14px;
            border-radius: 8px;
            border: 2px solid #80deea; /* Light teal border */
            font-size: 16px;
            background-color: rgba(255, 255, 255, 0.8); /* Slightly transparent white */
            color: #333;
            transition: border-color 0.3s;
        }

        .search-bar input::placeholder {
            color: #aaa;
        }

        .search-bar input:focus {
            border-color: #0097a7; /* Darker teal on focus */
            outline: none;
        }

        .filter-buttons {
            display: flex;
            gap: 12px;
        }

        .filter-btn {
            padding: 12px 20px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 14px;
            background-color: rgba(0, 188, 212, 0.7); /* Set background to transparent cyan */
            color: #fff;
            transition: background-color 0.3s, transform 0.3s;
            border: none;
        }

        .filter-btn:hover {
            background-color: rgba(0, 150, 136, 0.7); /* Darker shade on hover */
            transform: scale(1.05);
        }

        .books-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .book-card {
            background: rgba(255, 255, 255, 0.9); /* Slightly transparent white */
            border-radius: 10px;
            padding: 20px;
            position: relative;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .book-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15);
        }

        .book-icon {
            font-size: 3rem;
            color: #00796b; /* Teal color */
            margin-bottom: 15px;
        }

        .book-title {
            font-weight: bold;
            font-size: 1.6rem;
            margin: 8px 0;
        }

        .book-author {
            font-style: italic;
            font-size: 1.1rem;
            color: #555;
            margin-bottom: 10px;
        }

        .book-price {
            color: #ffb300; /* Amber color */
            font-weight: bold;
            font-size: 1.3rem;
            margin-bottom: 15px;
        }

        .book-status {
            position: absolute;
            top: 15px;
            right: 15px;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 12px;
            text-transform: uppercase;
            font-weight: bold;
        }

        .status-active {
            background-color: #388e3c; /* Green color */
            color: #fff;
        }

        .status-inactive {
            background-color: #d32f2f; /* Red color */
            color: #fff;
        }

        .book-actions {
            display: flex;
            justify-content: space-around;
            margin-top: 15px;
        }

        .book-actions button {
            padding: 10px;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.3s;
        }

        .edit-btn {
            background-color: rgba(0, 188, 212, 0.7); /* Set background to transparent cyan */
            color: #fff;
            font-weight: bold; /* Added bold font */
            padding: 10px 15px; /* Added padding */
            border-radius: 5px; /* Updated border radius */
        }

        .edit-btn:hover {
            background-color: rgba(0, 150, 136, 0.7); /* Darker shade on hover */
            transform: scale(1.05);
        }

        .delete-btn {
            background-color: #d32f2f; /* Red color */
            color: #fff;
        }

        .toggle-btn {
            background-color: #388e3c; /* Green color */
            color: #fff;
        }

        .delete-btn:hover {
            background-color: #c62828; /* Darker red */
            transform: scale(1.05);
        }

        .toggle-btn:hover {
            background-color: #2e7d32; /* Darker green */
            transform: scale(1.05);
        }

        .nav-links {
            text-align: center;
            margin-top: 40px;
        }

        .nav-links a {
            color: #00796b; /* Teal color */
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1rem;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: #004d40; /* Darker teal */
        }

        .no-books {
            text-align: center;
            font-size: 1.5rem;
            color: #777;
            padding: 30px;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1><i class="fas fa-book-open"></i> Book Showcase</h1>

        <div class="top-bar">
            <form class="search-bar" action="/books" method="GET">
                <input type="text" name="search" placeholder="Search books..." value="<%= searchQuery %>">
                <input type="hidden" name="filter" value="<%= currentFilter %>">
                <button type="submit" class="filter-btn">Search</button>
            </form>

            <div class="filter-buttons">
                <a href="/books" class="filter-btn <%= currentFilter === 'all' ? 'active' : '' %>">All</a>
                <a href="/books?filter=active<%= searchQuery ? '&search=' + searchQuery : '' %>" 
                   class="filter-btn <%= currentFilter === 'active' ? 'active' : '' %>">Active</a>
                <a href="/books?filter=inactive<%= searchQuery ? '&search=' + searchQuery : '' %>" 
                   class="filter-btn <%= currentFilter === 'inactive' ? 'active' : '' %>">Inactive</a>
            </div>
        </div>

        <% if (books.length > 0) { %>
            <div class="books-grid">
                <% books.forEach(book => { %>
                    <div class="book-card">
                        <div class="book-status <%= book.isActive ? 'status-active' : 'status-inactive' %>">
                            <%= book.isActive ? 'Active' : 'Inactive' %>
                        </div>
                        <div class="book-icon">
                            <i class="fas fa-book"></i>
                        </div>
                        <div class="book-title"><%= book.bookName %></div>
                        <div class="book-author">by <%= book.authorName %></div>
                        <div class="book-price">$<%= book.price %></div>
                        <div class="book-actions">
                            <a href="/edit/<%= book.id %>" class="edit-btn">
                                <i class="fas fa-edit"></i> Edit
                            </a>
                            <form action="/toggle-status/<%= book.id %>" method="POST" style="display: inline;">
                                <button type="submit" class="toggle-btn">
                                    <i class="fas fa-toggle-on"></i>
                                    <%= book.isActive ? 'Deactivate' : 'Activate' %>
                                </button>
                            </form>
                            <form action="/delete/<%= book.id %>" method="POST" style="display: inline;">
                                <button type="submit" class="delete-btn" onclick="return confirm('Are you sure you want to delete this book?')">
                                    <i class="fas fa-trash"></i> Delete
                                </button>
                            </form>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="no-books">
                <p>No books found.</p>
            </div>
        <% } %>

        <div class="nav-links">
            <a href="/">Add New Book</a>
        </div>
    </div>
</body>

</html>
