<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #4b0082, #800080);
            font-family: 'Arial', sans-serif;
            color: #fff;
            padding-top: 50px;
        }

        .card {
            background: #fff;
            color: #333;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            padding: 30px;
            max-width: 500px;
            margin: auto;
        }

        .card-header {
            background: #6a0dad;
            color: #fff;
            font-size: 1.5rem;
            font-weight: bold;
            text-align: center;
            padding: 15px;
            border-top-left-radius: 12px;
            border-top-right-radius: 12px;
        }

        .form-control {
            border-radius: 8px;
        }

        .btn-custom {
            width: 100%;
            padding: 10px;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: bold;
            transition: 0.3s;
        }

        .btn-primary {
            background: #6a0dad;
            border: none;
        }

        .btn-primary:hover {
            background: #580c99;
        }

        .btn-secondary {
            background: #444;
            border: none;
        }

        .btn-secondary:hover {
            background: #222;
        }
    </style>
</head>

<body>
    <div class="card">
        <div class="card-header">Edit Movie</div>
        <div class="card-body">
            <form action="/UpdateMovie" method="post" enctype="multipart/form-data">
                <input type="hidden" name="editid" value="<%= movie.id %>">
                <div class="mb-3">
                    <label class="form-label">Movie Name</label>
                    <input type="text" class="form-control" name="name" value="<%= movie.name %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Description</label>
                    <textarea class="form-control" name="description" rows="3" required><%= movie.description %></textarea>
                </div>
                <div class="mb-3">
                    <label class="form-label">Ticket Price</label>
                    <input type="number" class="form-control" name="price" value="<%= movie.price %>" step="0.01" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Movie Image</label>
                    <input type="file" class="form-control" name="image">
                    <% if (movie.image) { %>
                    <img src="<%= movie.image %>" alt="Current Image" class="img-preview mt-2" width="100%">
                    <% } %>
                </div>
                <button type="submit" class="btn btn-primary btn-custom">Update Movie</button>
                <a href="/" class="btn btn-secondary btn-custom mt-2">Back to Movies</a>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
