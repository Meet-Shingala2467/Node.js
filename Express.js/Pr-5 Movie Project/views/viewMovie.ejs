<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Collection</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #1e1e2f, #252540);
            color: #fff;
            padding: 40px 0;
        }

        .container {
            max-width: 1200px;
            background: rgba(37, 37, 64, 0.9);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
        }

        h1 {
            text-align: center;
            font-size: 3rem;
            font-weight: bold;
            margin-bottom: 30px;
        }

        .movie-card {
            background: rgba(58, 58, 93, 0.9);
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.4);
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .movie-card:hover {
            transform: scale(1.08);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.5);
        }

        .movie-card img {
            width: 100%;
            border-radius: 12px;
            margin-bottom: 15px;
        }

        .movie-card h5 {
            font-size: 1.75rem;
            margin-bottom: 10px;
            color: #ffcc00;
        }

        .movie-card p {
            font-size: 1.1rem;
            color: #dcdcdc;
        }

        .btn-container {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }

        .btn-custom {
            padding: 12px 24px;
            border-radius: 30px;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            font-weight: bold;
        }

        .btn-primary {
            background-color: #ff7b54;
            border: none;
        }

        .btn-primary:hover {
            background-color: #ff5733;
            transform: scale(1.1);
        }

        .btn-danger {
            background-color: #e63946;
            border: none;
        }

        .btn-danger:hover {
            background-color: #c91d2a;
            transform: scale(1.1);
        }

        .btn-success {
            background-color: #06d6a0;
            border: none;
        }

        .btn-success:hover {
            background-color: #04b383;
            transform: scale(1.1);
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Movie Collection</h1>
        <div class="text-end mb-4">
            <a href="/add" class="btn btn-success btn-custom">➕ Add Movie</a>
        </div>
        <div class="row g-4">
            <% movies.map((movie) => { %>
                <div class="col-md-6 col-lg-4">
                    <div class="movie-card">
                        <img src="<%= movie.image %>" alt="<%= movie.name %>">
                        <h5><%= movie.name %></h5>
                        <p><%= movie.description %></p>
                        <p>💰 Price: ₹<%= movie.price %></p>
                        <div class="btn-container">
                            <a href="/deleteMovie?deletId=<%= movie.id %>" class="btn btn-danger btn-custom">🗑 Delete</a>
                            <a href="/editMovie?editId=<%= movie.id %>" class="btn btn-primary btn-custom">✏ Edit</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
