<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Movie</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #4b0082, #800080);
            font-family: 'Arial', sans-serif;
            color: #fff;
            padding-top: 50px;
        }

        .card {
            background: #fff;
            color: #333;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            padding: 30px;
            max-width: 500px;
            margin: auto;
        }

        .card-header {
            background: #6a0dad;
            color: #fff;
            font-size: 1.5rem;
            font-weight: bold;
            text-align: center;
            padding: 15px;
            border-top-left-radius: 12px;
            border-top-right-radius: 12px;
        }

        .form-control {
            border-radius: 8px;
        }

        .btn-custom {
            width: 100%;
            padding: 10px;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: bold;
            transition: 0.3s;
        }

        .btn-primary {
            background: #6a0dad;
            border: none;
        }

        .btn-primary:hover {
            background: #580c99;
        }

        .btn-secondary {
            background: #444;
            border: none;
        }

        .btn-secondary:hover {
            background: #222;
        }
    </style>
</head>

<body>
    <div class="card">
        <div class="card-header">Add Movie</div>
        <div class="card-body">
            <form action="/insertMovie" method="post" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="movieName" class="form-label">Movie Name</label>
                    <input type="text" id="movieName" class="form-control" name="name" required>
                </div>
                <div class="mb-3">
                    <label for="movieDescription" class="form-label">Description</label>
                    <textarea id="movieDescription" class="form-control" name="description" rows="3" required></textarea>
                </div>
                <div class="mb-3">
                    <label for="ticketPrice" class="form-label">Ticket Price</label>
                    <input type="number" id="ticketPrice" class="form-control" name="price" required>
                </div>
                <div class="mb-3">
                    <label for="movieImage" class="form-label">Movie Image</label>
                    <input type="file" id="movieImage" class="form-control" name="image" accept="image/*">
                </div>
                <button type="submit" class="btn btn-primary btn-custom">Submit</button>
                <a href="/" class="btn btn-secondary btn-custom mt-2">View Movies</a>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
